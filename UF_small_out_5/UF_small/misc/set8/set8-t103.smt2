(set-logic UF)
(declare-sort Set 0)
(declare-sort Elem 0)
(declare-fun member (Elem Set) Bool)
(declare-fun subset (Set Set) Bool)
(declare-fun seteq (Set Set) Bool)
(declare-fun union (Set Set) Set)
(declare-fun intersection (Set Set) Set)
(declare-fun difference (Set Set) Set)
(declare-fun a () Set)
(declare-fun b () Set)
(assert (not (not (subset (intersection b a) (intersection a b)))))
(assert (or (and (subset (intersection a b) (intersection b a)) (subset (intersection b a) (intersection a b))) (not (subset (intersection a b) (intersection b a))) (not (subset (intersection b a) (intersection a b)))))
(assert (not (and (subset (intersection a b) (intersection b a)) (subset (intersection b a) (intersection a b)))))
(assert (subset (intersection a b) (intersection b a)))
(check-sat)
(exit)
