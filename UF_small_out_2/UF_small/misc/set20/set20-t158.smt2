(set-logic UF)
(declare-sort Set 0)
(declare-sort Elem 0)
(declare-fun member (Elem Set) Bool)
(declare-fun subset (Set Set) Bool)
(declare-fun seteq (Set Set) Bool)
(declare-fun union (Set Set) Set)
(declare-fun intersection (Set Set) Set)
(declare-fun difference (Set Set) Set)
(declare-fun a () Set)
(declare-fun b () Set)
(assert (not (not (subset (difference a (difference a b)) (intersection a b)))))
(assert (or (and (subset (intersection a b) (difference a (difference a b))) (subset (difference a (difference a b)) (intersection a b))) (not (subset (intersection a b) (difference a (difference a b)))) (not (subset (difference a (difference a b)) (intersection a b)))))
(assert (not (and (subset (intersection a b) (difference a (difference a b))) (subset (difference a (difference a b)) (intersection a b)))))
(assert (subset (intersection a b) (difference a (difference a b))))
(check-sat)
(exit)
